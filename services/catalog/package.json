{
  "name": "@kairos/catalog",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Catalogue persistence — the gateway's skill/workflow/MCP store, optionally backed by a JSON file so a restart stops losing rows.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build step'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/node": "^22"
  }
}
