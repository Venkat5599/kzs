{
  "name": "@kairos/workflow",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Workflow graph engine — validation and deterministic execution of the node/edge graphs the dashboard edits.",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'no build step'",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'no lint configured'",
    "test": "bun test"
  },
  "dependencies": {
    "@kairos/shared": "workspace:*"
  }
}
